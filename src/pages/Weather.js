import GetLocationData from "../components/GetLocationData";

const Weather = () => {
  return (
    <section>
      <h1>The Weather Page</h1>
      <GetLocationData />
    </section>
  );
};

export default Weather;
